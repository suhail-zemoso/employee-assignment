const { messages } = require("./apiResponse");
const { userTypeValue, userStatusValue, doctorStatusValue, appointmentStatusValue } = require("./dbEnums");

module.exports = {
    messages,
    userTypeValue,
    userStatusValue,
    doctorStatusValue,
    appointmentStatusValue
};
